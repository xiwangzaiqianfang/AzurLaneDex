<Page
    x:Class="AzurLaneDex.Views.ManageAccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
	xmlns:converters="using:AzurLaneDex.Converters"
    NavigationCacheMode="Disabled">

	<Page.Resources>
		<converters:ImagePathToBitmapConverter x:Key="ImagePathToBitmap"/>
		<converters:BoolToRoleTextConverter x:Key="BoolToRoleText"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
		<Style x:Key="SettingsCardStyle" TargetType="Border">
			<Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}"/>
			<Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="8"/>
			<Setter Property="Padding" Value="16"/>
			<Setter Property="Margin" Value="0,0,0,0"/>
		</Style>
	</Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- 顶部导航栏 -->
		<StackPanel Orientation="Horizontal" Margin="20,20,20,12" Spacing="12">
            <Button x:Name="BackButton" Click="BackButton_Click" Content="帐户" FontSize="28" FontWeight="SemiBold" Margin="0,2,0,0" Background="Transparent" BorderThickness="0" ToolTipService.ToolTip="返回" VerticalAlignment="Center"/>
            <FontIcon Grid.Column="0" Glyph="&#xE974;" FontFamily="Segoe Fluent Icons" FontSize="22" Margin="-20,3,-10,0" VerticalAlignment="Center"/>
            <TextBlock Text="你的账户" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
		</StackPanel>

		<ScrollViewer Grid.Row="1">
            <StackPanel Margin="20" Spacing="16">
                <!-- 添加帐户卡片 -->
				<Border Style="{StaticResource SettingsCardStyle}">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<StackPanel Grid.Column="0" VerticalAlignment="Center">
							<TextBlock Text="添加其他用户" FontSize="14"/>
						</StackPanel>
						<Button Grid.Column="1" x:Name="AddAccountButton" Click="AddAccountButton_Click" Style="{StaticResource AccentButtonStyle}">
							<StackPanel Orientation="Horizontal" Spacing="4">
								<TextBlock Text="添加帐户" Margin="10,0,10,0"/>
							</StackPanel>
						</Button>
					</Grid>
				</Border>
                <TextBlock Text="已添加的账户" FontSize="16" FontWeight="SemiBold"/>
                <!-- 账户列表（使用 ItemsRepeater 或 ListView） -->
				<controls:ItemsRepeater x:Name="AccountsRepeater">
					<controls:ItemsRepeater.ItemTemplate>
						<DataTemplate>
							<Border Background="{ThemeResource CardBackgroundFillColorDefault}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="8" Margin="0,0,0,12" Padding="0">
								<Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
									<Expander.Header>
										<Grid Padding="0,12" MinHeight="64">
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="Auto"/>
												<ColumnDefinition Width="*"/>
												<ColumnDefinition Width="Auto"/>
											</Grid.ColumnDefinitions>
											<Border Width="32" Height="32" CornerRadius="16" Background="LightGray" Margin="0,0,12,0" VerticalAlignment="Center">
												<Image Source="{Binding AvatarPath, Converter={StaticResource ImagePathToBitmap}}"
                                                       Width="32" Height="32" Stretch="UniformToFill"/>
											</Border>
											<StackPanel Grid.Column="1" VerticalAlignment="Center">
												<TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/>
												<TextBlock Text="{Binding IsDeveloper, Converter={StaticResource BoolToRoleText}}"
                                                           FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
											</StackPanel>
											<!-- 右箭头（Expander 自带，不需要额外控件） -->
										</Grid>
									</Expander.Header>
									<StackPanel Margin="0,0,0,0" Spacing="12">
										<TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
											<Run Text="最后登录时间: "/><Run Text="{Binding LastLogin, FallbackValue='从未'}"/>
										</TextBlock>
                                        <TextBlock Text="是否为管理员帐户"/>
                                        <ToggleSwitch OffContent="否" OnContent="是" Margin="0,0,0,0"
													  IsOn="{Binding IsDeveloper, Mode=OneWay}"
													  Toggled="AdminToggle_Toggled" Tag="{Binding}"
                                                      Visibility="{Binding IsAdminVisible, Converter={StaticResource BoolToVisibility}}"/>
										<Button Content="删除此账户" Click="DeleteAccountButton_Click" Tag="{Binding}" Foreground="Red" HorizontalAlignment="Left"/>
									</StackPanel>
								</Expander>
							</Border>
						</DataTemplate>
					</controls:ItemsRepeater.ItemTemplate>
				</controls:ItemsRepeater>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>