<Page x:Class="AzurLaneDex.Views.StatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="20,20,20,12" Spacing="12">
            <!-- 标题 -->
            <TextBlock Text="统计概览" FontSize="24" FontWeight="SemiBold" Margin="10,10,0,0" VerticalAlignment="Center"/>

            <!-- 收集进度条 -->
            <StackPanel Margin="0,0,0,16">
                <TextBlock x:Name="CollectionProgressText" VerticalAlignment="Center" Margin="10,10,0,5"/>
                <ProgressBar x:Name="CollectionProgressBar" Height="6" Margin="0,8,0,0"/>
            </StackPanel>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Margin="20,0,0,0">
            <!-- 统计卡片网格 -->
            <controls:ItemsRepeater x:Name="StatsRepeater">
                <controls:ItemsRepeater.Layout>
                    <controls:UniformGridLayout MinItemWidth="174" 
                                                MinRowSpacing="15" 
                                                MinColumnSpacing="15"/>
                </controls:ItemsRepeater.Layout>
                <controls:ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefault}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="16"
                                MinWidth="150" MinHeight="80">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Value}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </controls:ItemsRepeater.ItemTemplate>
            </controls:ItemsRepeater>
        </ScrollViewer>
        <Button Content="导出为图片" Grid.Row="2" Click="ExportToImage" HorizontalAlignment="Center" Margin="0,0,0,20"/>
    </Grid>
</Page>